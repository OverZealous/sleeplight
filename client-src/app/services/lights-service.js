export default angular.module('sleeplight.services.lights-service', [])

	.factory('lightsService', function($http) {

		const current = null;

		return {
			get current() {
				if(current === null) {

				}
			}
		}
	});
